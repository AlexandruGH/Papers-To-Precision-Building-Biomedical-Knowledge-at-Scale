# Pinned-ish requirements for a full developer environment.
# For a minimal offline demo, `pip install -e .` is enough (core deps only).
# Install the full stack with:  pip install -e ".[all]"

# --- core ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
networkx>=3.1
pyyaml>=6.0
tqdm>=4.66

# --- nlp / encoders ---
sentence-transformers>=2.6
gliner>=0.2.6
transformers>=4.40
torch>=2.2

# --- ingestion ---
biopython>=1.83

# --- graph stores ---
kuzu>=0.4
neo4j>=5.20

# --- graph neural networks ---
torch-geometric>=2.5
pcst_fast>=1.0.10

# --- local llm synthesis (Ollama + structured outputs; no API key) ---
instructor>=1.5
openai>=1.30

# --- visualisation ---
pyvis>=0.3.2
matplotlib>=3.8
seaborn>=0.13
